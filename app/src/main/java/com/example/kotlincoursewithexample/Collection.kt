package com.example.kotlincoursewithexample

fun main() {
  //List
   //Mutable list element can change the data add or remove data
    //Immutable list can't be change
  //  val num = listOf(1,2,3)
    val num = mutableListOf(1,2,3)
    println(num.indexOf(3))
    println(num.contains(4))

    //map
    //key value pair is map

}
